# DEPRECATED: This file is deprecated in favor of strategy-specific services
# For production use, please use one of the following:
# - Blue-Green: kubernetes/frontend-service-blue-green.yaml
# - Canary: kubernetes/frontend-service-canary.yaml
# See docs/DEPLOYMENT_GUIDE.md for detailed instructions

apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: NodePort # Expose frontend for external access
