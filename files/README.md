# Sample Files

This folder stores reusable payloads for demos and local testing.

- `files/kafka/sample-events.jsonl` contains Kafka event envelopes for the demo producer.
- `files/elk/budget-events.ndjson` contains log events for the ELK pipeline.
- `files/grpc/requests.json` contains example gRPC request bodies.
